import { Fragment } from "react";
import { SearchBar } from "../../components";

export const Store = () => {
    return (
        <Fragment>
            <span style={{ margin: "5rem" }}></span>
            <SearchBar />
        </Fragment>
    )
}